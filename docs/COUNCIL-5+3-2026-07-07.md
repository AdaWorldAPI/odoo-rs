# 5+3 Council — verdict on "the odoo→odoo-rs transpile is COMPLETE" (main @ 7b5b5f2)

> 5 Sonnet review lenses + Opus 3-meta synthesis, 2026-07-07. Auto-resolves
> landed same-session in `5b45170`. Append-only; regrades in place.

## VERDICT: **LAND — with the named tail below** (was OVERCLAIMED before the same-session auto-resolves)

The claim survives at its intended meaning: real Odoo source lowers through
the OGAR V3 compile substrate to `CompiledClass{class, facet, actions}` and
materializes via the SDK — canonical kausal, zero SurrealQL, proven on the
REAL corpus, drift-fused. "Complete" is honest **only with** the tail table
below attached; the council found and killed one genuine overclaim.

## Lens verdicts

| Lens | Verdict | Disposition |
|---|---|---|
| R1 toolchain honesty | HOLD → **RESOLVED** (`5b45170`) | 8 un-allowed deprecated sites → 0 warnings; `--actions` dependency on the witness named in the deprecation note (no removal until migrated); stale corpus count 2 739 → 3 065. Remaining posture note: **no CI** — "suite green" is point-in-time by design (never-pin-bump, fail-loud); a CI lane with sibling checkouts is optional hardening. |
| R2 baton | CATCH-LATENT ×2 | queued (see Q3, Q7) |
| R3 doctrine | TARGETS-CORE + 1 VIOLATION | `triple.rs` duplicates upstream — queued as THE next refactor (Q1); `alignment.rs` constants = RESIDUE (documented, phase-3 upstream push already planned) |
| R4 convergence | 3 OPPORTUNITY / 3 WORTH-EXPLORING, 0 DROP | queue Q2/Q5/Q6; one stale premise corrected below |
| R5 completeness | OVERCLAIMED → **core gap RESOLVED** (`5b45170`) | toy-fixture gap killed: the REAL `account` addon compiles (55 files → 71 models / 1 496 ActionDefs / 347 kausal); verbatim 7 380-line `account_move.py` vendored + exact-fused (1 model, classid `0x0202_0002`, 104 attrs / 38 assocs / 93 computed / 354 actions / 92 kausal). Rest → tail. |

## Council correction of a reviewer premise (3-meta)

R4's Seam-3 blocker "L6 is unruled (`odoo ?`)" is **stale paperwork, not a
missing ruling**: the operator ruled L6 = 3×(8:8:8:8) SPOG for Odoo
(2026-07-06 briefing + in-session confirmation; recorded in the sprint doc and
PR #21/#24). What's genuinely missing is (a) the lance-graph
`le-contract.md` table updating the `[H]` mark (ledger paperwork, Q4) and
(b) the attribute→byte-position assignor (real code, no owner yet) — R4's
"no assignor exists for ANY layout" stands.

## The queue (owners = next session unless noted)

| # | Item | Size | Source |
|---|---|---|---|
| Q1 | **`triple.rs` → consume upstream `ruff_spo_triplet`** (closed-vocab fail-loud `from_ndjson`; order-preserving `corpus::reassemble`); repoint `inheritance`/`mro`/`recompute_dag`/`relations`/`ogar_actions`; file `selection_value` as the one missing upstream predicate | medium refactor | R3 VIOLATION; also unlocks R2-b + R4-Seam-1 |
| Q2 | **ruff: `EmittedBy` arm in `corpus::group_functions`** — the smallest, highest-leverage diff in the set: unlocks recipe-centroid classification over the ALREADY-COMMITTED slice_2 corpus (388 `emitted_by` rows currently invisible to `recipe::classify`) | ~10 lines, ruff PR | R4 Seam 6 (OPPORTUNITY) |
| Q3 | ruff: `ACTIONDEF_FEED_FUNCTION` doc/name fix — `guarded_writes`' consumer is same-repo `recipe::classify`, not the cross-repo ActionDef feed | doc-only, ruff PR | R2-a |
| Q4 | **Ledger appends**: OGAR board (W1 kausal-parity, W3 fork delete, transpile completion, the F1 "3 manifest hierarchies C3-inconsistent" finding that was never filed); refresh `docs/knowledge/OGAR-VOCAB-SURFACE.md` (says kausal never populated — false at OGAR HEAD); lance-graph `le-contract.md` L6 ruling paperwork | docs-only, 3 repos | R5-4, council correction |
| Q5 | **od-posting re-architecture** — its 4 invariants are written in SurrealQL vocabulary (`DEFINE EVENT/SEQUENCE`, surrealdb txns) against the substrate this repo killed; retarget to the PG facet table (`emit_facet_table_ddl`) / V3 semantics before any implementation | design doc rewrite | R5-5 (sharpest unflagged gap) |
| Q6 | Relocate `mint_mask`/`mint_wide_mask` (~60 domain-blind lines) from od-ontology to `lance_graph_contract::class_view` — Rails/ERB (`ViewFieldSet`) then mints the same `WideFieldMask` for free; + the `odoo_actions` authority table (~100-150 lines by the `ocr_actions` precedent, or harvested via `compile_source`) when W2′ storage starts | 2 small upstream PRs | R4 Seams 4+5 (OPPORTUNITY) |
| Q7 | `corpus_to_actions` bare-`raises` guard vs canonical `RecipeCentroid::Guard` (`raises ∧ ¬writes ∧ ¬calls`): after Q1, reconstruct `Function` and call `recipe::classify`, or document the exemption on the witness | small, after Q1 | R2-b |
| Q8 | **The 5.1% tail (18 order-dependent hooks)** — currently unowned. Doctrine says: essential-imperative → hand-port (the true 15%→5% residue), never recipe-forced. Owner: the od-posting/behaviour lane when it restarts; acceptance = each hook either hand-ported or explicitly escaped with a name | design decision | R5-2 |
| Q9 | Upstream carrier asks (OGAR): row #11 `Association` stored-vs-VALUE-projection marker; row #15 `body_source` (AT-CARRY-3 / SPEC-1 Part A); row #17 THROW exception-type slot; + a `store=True`-through-`compile_source` verify test for row #10 (`ComputedField.stored` is now set upstream — carried, not yet verified) | 3 small OGAR asks | R5-3 |

## What the council proved (not just claimed)

- F15 45.7% / F16 21.0% / F17 94.9% drift-fuses re-run live and match the
  docs verbatim (R1). No vacuous tests, no `#[ignore]`, no fabricated greens.
- Classid boundary canon-high both sides, zero legacy literals (R2).
- `Facet` ↔ `FacetCascade` byte-identity holds at current tips, self-tested
  on both sides (R2).
- The kausal-parity mechanism (corpus witness vs OGAR canonical) is exactly
  the collapse Seam 2 wants, already proven in miniature (R4 on W1).
- The V3 pipeline handles the real 7 380-line `account_move.py` and the full
  55-file account addon without a panic, first try (R5 → resolved).
